import React from "react";
import { signInWithGoogle, signOut } from "../../firebaseConfig";

<><button onClick={signInWithGoogle}>Sign in with Google</button><button onClick={signOut}>Sign out</button></>